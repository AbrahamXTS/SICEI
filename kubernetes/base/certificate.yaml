apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: frontend-certificate
  namespace: sicei
spec:
  secretName: frontend-certificate
  dnsNames:
    - sicei.abrahamespinosa.net
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
